# 投资记忆

> 记录投资者特质层面的演变：偏好漂移、弱点变化、行为模式固化/纠正。
> 追加式，每条目以 `## YYYY-MM-DD` 开头，不覆盖历史。
> 每次生成投资分析前必须读取。
>
> **给 AI 的规则**：这里的核心不是「记流水账」，而是追踪两条线——① **弱点清单如何演变**（哪些弱点在改善、哪些还没解决）；② **待验证模式是否兑现**（上次你猜他会怎么做，这次他真做了吗）。用 `✅` / `❓` / `❌` 标记验证结果，让「越用越懂你」真正落地。

---

## YYYY-MM-DD（首次建档）

### 观察到的行为模式

1. [待填写：如「极保守的资产配置」「持有亏损标的的倾向（Loss Aversion）」「过度分散化」「缺少核心宽基」等，每条写清楚「观察到的现象」+「可能的心理原因」]

### 投资知识水平推断

[待填写：如「了解 QDII/ETF/FOF 等工具 → 有一定投资知识；但现金占比过高、缺少宽基 → 资产配置理论上需加强」。判断口径：工具熟悉度 + 配置意识 + 纪律性（是否有定投）。]

### 弱点清单（首次，后续每次更新在这里迭代）

| # | 弱点 | 状态 | 备注 |
|---|------|------|------|
| 1 | [待填写] | 存在 / 改善中 / 已解决 | |
| 2 | [待填写] | | |

### 后续关注

[待填写：列出未来要观察的行为信号，例如「是否会减少现金冗余」「对亏损标的的态度」「是否会增加宽基配置」。这些会在下次复盘时被验证。]

---

## 追加条目模板（后续每次有新发现时，照此结构追加）

### 观察到的行为模式

- [待填写]

### 验证上次的「待验证模式」

- ✅ [待填写：兑现的假设，写明证据]
- ❓ [待填写：仍在观察中]
- ❌ [待填写：被推翻的假设，写明为什么]

### 更新后的弱点清单

| # | 弱点 | 状态变化 | 备注 |
|---|------|----------|------|
| 1 | ~~旧表述~~ → 新表述 | 改善中 / 未解决 | [待填写] |

### 新的待验证模式

- [待填写：本次新产生的、下次要验证的假设]
